apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: example-api
  namespace: tenant2-flux
spec:
  interval: 1m
  serviceAccountName: tenant2-flux-sa
  chart:
    spec:
      chart: gitops/charts/flux-example/
      sourceRef:
        kind: GitRepository
        name: git-repository-tenant2
