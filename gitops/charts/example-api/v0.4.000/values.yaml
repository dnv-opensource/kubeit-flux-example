platform-service:
  app: admin-api
  version: v1

  gateway:
    exposeService: true
    name: istio-system/global-https-internal-default-gateway
  fluxAutomation:
    enabled: true
    provider: azure
    interval: 1m
    
