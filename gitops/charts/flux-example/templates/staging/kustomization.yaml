{{- if .Values.environments.prod.enabled }}
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: stage-cluster
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "flux-bootstrap.labels" . | nindent 4 }}
    environment: stage
spec:
  interval: {{ .Values.environments.staging.syncInterval }}
  retryInterval: {{ .Values.environments.staging.retryInterval }}
  path: {{ .Values.git.path }}/staging
  prune: {{ .Values.environments.staging.prune }}
  sourceRef:
    kind: GitRepository
    name: flux-system-stage
  targetNamespace: {{ .Values.environments.staging.namespace }}
{{- end }}
